input "type:touchpad" {
    tap enabled
    natural_scroll enabled

    # Disable while typing
    dwt enabled

    accel_profile "flat"
    pointer_accel 1.0
}

input "type:keyboard" {
    xkb_options "caps:ctrl_modifier"
}
